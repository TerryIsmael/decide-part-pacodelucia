export 
class Question {
    constructor(desc, options) {
        this.desc = desc
        this.options = options
    }
}

export
class Option {
    constructor(number, option) {
        this.number = number
        this.option = option
    }

}